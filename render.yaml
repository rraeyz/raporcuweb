services:
  - type: web
    name: raporcuweb
    env: python
    region: frankfurt
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: raporcuweb-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.0

databases:
  - name: raporcuweb-db
    region: frankfurt
    plan: free
    databaseName: raporcuweb
    user: raporcuweb
